load("@npm//@bazel/typescript:index.bzl", "ts_project")

ts_project(
    name = "app",
    out_dir = "build",
    root_dir = "src",
    supports_workers = True,
    tsconfig = {
        "compilerOptions": {
            "declaration": True,
            "lib": ["ES5"],
            "skipLibCheck": True,
            "types": [],
            "noEmitOnError": True,
        },
    },
    deps = [
        "//packages/lib",
    ],
)
